<nav class="navbar navbar-expand-lg bg-dark navbar-dark navbar-fixed">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{ path('app_home2')}}"><span class="grande-taille">TUKKI</span></a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav w-100">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="{{ path('app_home2')}}">Accueil</a>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">Services</a>
         {{render(controller('App\\Controller\\FrontHotelController::renderHotelDropDOown')) }}
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#"><i class="icon text-danger" data-feather="home"></i></a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="">Plages</a>
        </li>

         <li class="nav-item">
          <a class="nav-link" href="">Parc</a>
        </li>


 
      <li class="nav-item ms-lg-auto d-flex align-items-center">
          {% if app.user is null %}
          <a class="nav-link" href="{{ path('app_register')}}">Inscription</a>
          {% else %}
          <span class="text-light me-3">{{ app.user.email}}</span>
          {% if app.user.avatar is not null and app.user.avatar.imageName is not null %}
          <img src="{{ asset('images/avatar/' ~ app.user.avatar.imageName) }}" alt="avatar" class="avatar">
          {% endif %}
          <a class="nav-link" href="{{ path('app_front_user') }}">Mon Compte</a>
          {% endif %}
      </li>
      {# on vérifie si l'utilisateur est un admin #}
          {% if is_granted("ROLE_ADMIN") %}
            <li class="nav-item">
            <a class="nav-link" href="{{ path('app_admin')}}">Admin</a>
            </li>
          {% endif %}

        <li class="nav-item">
        {% if is_granted("ROLE_USER") %}
          <a class="nav-link" href="{{ path('app_logout')}}">Déconnexion</a>
          {% else %}
          <a class="nav-link" href="{{ path('app_login')}}">Connexion</a>
          {% endif %}
        </li>
      </ul>
    </div>
  </div>
</nav>  


