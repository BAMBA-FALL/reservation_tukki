{% extends 'base-backend.html.twig'%}

{% block title %}Edit Hotel{% endblock %}

{% block body %}
     <a href="{{ path('app_admin_hotel_index') }}" class="btn btn-dark btn-sm mb-2">Retour à la liste</a>
    <div class="d-flex justify-content-between align-items-center">
      <h1>Modifier</h1>
  {{ include('admin_hotel/_delete_form.html.twig') }}
    </div>
  
    {{ include('admin_hotel/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

   

  
{% endblock %}
