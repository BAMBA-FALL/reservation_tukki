{% extends 'base-backend.html.twig' %}

{% block title %}Image{% endblock %}

{% block body %}
    <a href="{{ path('app_admin_image_index') }}" class="btn btn-dark btn-sm mb-2">Retour à la liste</a>
    <h1>Image</h1>
    

    <table class="table talbe-index table-striped table-responsive">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ image.id }}</td>
            </tr>
            <tr>
                <th>ImageName</th>
                <td>{{ image.imageName }}</td>
            </tr>
            <tr>
                <th>RankOrder</th>
                <td>{{ image.rankOrder }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ image.updatedAt ? image.updatedAt|date('m-d-Y H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>



    <a href="{{ path('app_admin_image_edit', {'id': image.id}) }}" class="btn btn-success">Modifier</a>

    {{ include('admin_image/_delete_form.html.twig') }}
{% endblock %}
